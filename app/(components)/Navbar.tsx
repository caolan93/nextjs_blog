type Props = {};

const Navbar = (props: Props) => {
  return (
    <header className="mb-5">
      <nav className="flex justify-between items-center">Nav</nav>
    </header>
  );
};

export default Navbar;
